# teams/ai — AI Synthesis (private)

Output of the wiki **synthesis** pass (`scripts/wiki_compile.py --level full`).
All-seeing, extended-thinking connections across the entire EKB corpus.

- **Access tier:** `ai` — visible only to CEO, CFO, and super-admin in the EKB Explorer.
- **Private repo:** like `teams/cfo/` and `teams/ceo/`, this is a separate git repo
  gitignored from the main public repo. All-seeing synthesis reads CFO/CEO private
  data, so its output must never land in the general/public tier.
- `synthesis/` — one page per synthesized connection, plus `index.md` and `log.md`.

Humans review these; strong findings may "graduate" into a structured wiki section.
